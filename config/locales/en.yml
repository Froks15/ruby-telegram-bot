en:
  errors:
    unknow_error: |
      Something went wrong.
  geocoder:
    not_found:
      content: |
        Unknown place. Please try again.
  telegram_webhooks:
    action_missing:
      command: "Unknown command: /%{command}"
    help:
      content: |
        Send the city name
    start:
      content: |
        Just send the city name
    message:
      content: |
        Hi, %{user_name}

        %{city_name} %{country_emoji}

        Morning: %{morning_temperature} °C %{morning_emoji}
        Afternoon: %{day_temperature} °C %{day_emoji}
        Evening: %{evening_temperature} °C %{evening_emoji}